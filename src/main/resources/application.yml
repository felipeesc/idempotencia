spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pix_db
    username: pix_user
    password: pix_pass
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
