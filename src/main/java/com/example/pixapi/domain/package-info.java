/**
 * Camada de domínio: contém as entidades, regras de negócio e contratos (interfaces)
 * que representam o coração da aplicação, sem depender de frameworks ou infraestrutura.
 *
 * Exemplo: {@link com.example.pixapi.domain.Transferencia} modela uma transação PIX
 * com seus campos, status e invariantes (ex: valor não pode ser negativo).
 */
package com.example.pixapi.domain;
