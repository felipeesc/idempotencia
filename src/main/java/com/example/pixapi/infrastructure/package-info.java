/**
 * Camada de infraestrutura: implementações técnicas que suportam a aplicação —
 * banco de dados, HTTP, filas, integrações externas e mocks.
 *
 * Tudo aqui é detalhe de implementação; o domínio e a aplicação não dependem deste pacote.
 */
package com.example.pixapi.infrastructure;
