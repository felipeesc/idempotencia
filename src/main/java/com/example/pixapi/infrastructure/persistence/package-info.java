/**
 * Persistência: repositórios JPA responsáveis por ler e gravar dados no PostgreSQL.
 *
 * Exemplo: {@link com.example.pixapi.infrastructure.persistence.TransferenciaRepository}
 * busca uma transferência pela chave de idempotência para evitar processamento duplicado.
 */
package com.example.pixapi.infrastructure.persistence;
